# S9 — Mobile (Android) — Audit + Fix Queue

**Date:** 2026-04-21
**Squad:** S9 — Mobile (Android Kotlin)
**Status:** F0 (in progress)

## Tree

- Module: `mobile/android/app/`
- Toolchain: AGP 8.x + Kotlin 2 + Compose BOM 2024.09.03 + Hilt 2.52
  + Retrofit 2.11 + OkHttp 4.12 + Moshi 1.15 + Supabase GoTrue 2.6 +
  Coroutines 1.9 + DataStore 1.1.
- Source set: 68 Kotlin files, 13 629 LOC.
- minSdk 26, targetSdk 34, JVM 17. `compose=true`,
  `buildConfig=true`. R8 disabled in `release` (commented as a
  one-line flip).
- Tooling on host: JDK 17 (Temurin 17.0.18) ✅, Gradle 8.10.2 (cached
  wrapper) ✅, Android cmdline-tools at
  `/opt/homebrew/share/android-commandlinetools` ✅.

## Existing test layout

- `app/src/test/...`: **MISSING** (no JVM unit tests at all).
- `app/src/androidTest/...`: **MISSING** (no instrumentation tests).
- `build.gradle.kts` already wires `junit:4.13.2` for JVM tests and
  Espresso/Compose for instrumentation. Test source sets just need
  to exist.

## Risk register

| ID | Surface | Severity | Why |
| --- | --- | --- | --- |
| R1 | `data/network/RetryInterceptor.kt` (57 LOC) | HIGH | Backoff math + idempotency gate; silent regression replays writes. |
| R2 | `data/network/AuthInterceptor.kt` (26 LOC) | HIGH | Bearer-token injection; missing/extra header is a security/auth bug. |
| R3 | `data/auth/TokenStore.kt` (41 LOC) | MEDIUM | DataStore key constants — rename breaks every install. Requires Robolectric or instrumentation. |
| R4 | `data/network/PipelineSse.kt` (96 LOC) | MEDIUM | Event-shape parser inline in callback flow; stage/agent fallback (`agent` ↔ `agent_name`) not pinned. |
| R5 | `data/network/ParityModels.kt` (469 LOC) | LOW | Pure data classes, Moshi adapters generated by KSP. Snapshot golden JSON to catch field renames. |
| R6 | UI ViewModels (e.g. `JobBoardViewModel`, `ApplicationsViewModel`) | LOW | Coroutines-based; testable with `kotlinx-coroutines-test` but largely Compose-driven. Defer. |

## Fix queue

- **F0** — this audit doc.
- **F1** — pin `RetryInterceptor` invariants (R1) via JVM unit
  tests against a stub `Interceptor.Chain`. Bootstrap
  `app/src/test/java/...` source set.
- **F2** — pin `AuthInterceptor` header injection (R2) via JVM
  unit tests.
- **F3** — extract `PipelineSse` event-shape parsing into a pure
  helper (`fun parsePipelineEvent(type: String?, data: String): PipelineEvent`)
  and pin it (R4). Smallest viable refactor — keeps the SSE flow
  intact, just relocates the JSON map → event mapping.
- **F4** — Moshi adapter golden snapshot for `Application` /
  `GenerationJob` JSON shapes (R5).
- **F5** — ADR-0011 mobile contract surface + S9 sign-off.

## Out of scope (deferred with rationale)

- **R3 TokenStore** — datastore-preferences requires either
  Robolectric or instrumentation; both inflate suite cost
  significantly. Constants are stable in practice; revisit when
  Robolectric is wired for Compose previews.
- **Compose UI tests, screenshot tests, Lighthouse equivalents,
  Play-Store hardening, R8 enabling, signing config** — owned by
  S10 Infra & Deploy and the future Mobile-Release squad.
- **ViewModel coroutines tests** — large surface; not a contract
  pin but a behavioural-coverage task. Track separately.

## Verification gate

- `cd mobile/android && ./gradlew :app:testDebugUnitTest`. Expect
  GREEN once F1 lands. Wall time TBD on first cold run.
- Backend + frontend suites untouched.

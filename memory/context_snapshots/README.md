# memory/context_snapshots

**Generated**, gitignored. Output of `python -m scripts.memory.cli context`
when invoked with `--save`.

Future-use directory; today the CLI prints to stdout. Reserved so the
agent feedback loop (planned m12-pr22) has a stable place to write
the bundles it actually consumed.

Do not commit anything here.

# memory/graph

**Generated**, gitignored. Output of `python -m scripts.memory.cli graph`:

- `graph.json` — flat JSON with `nodes`, `edges`, `counts`

Schema: see [`../docs/KNOWLEDGE_GRAPH_DESIGN.md`](../docs/KNOWLEDGE_GRAPH_DESIGN.md).

Rebuild any time:

```bash
python -m scripts.memory.cli graph
```

Do not commit anything here.

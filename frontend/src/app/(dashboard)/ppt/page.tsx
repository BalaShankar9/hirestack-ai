import PPTGenerator from "@/features/ppt/PPTGenerator";

export const metadata = {
  title: "PPT Generator — HireStack",
  description: "Generate investor-grade presentations from a topic.",
};

export default function PPTPage() {
  return <PPTGenerator />;
}

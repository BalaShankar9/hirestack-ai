# generated by datamodel-codegen:
#   filename:  generation.completed.v1.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Literal
from uuid import UUID

from pydantic import BaseModel, ConfigDict, conint


class Status(StrEnum):
    succeeded = 'succeeded'
    failed = 'failed'
    cancelled = 'cancelled'


class Payload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    job_id: UUID
    application_id: UUID
    user_id: UUID
    status: Status
    duration_ms: conint(ge=0) | None = None
    error_class: str | None = None


class GenerationCompletedV1(BaseModel):
    """
    Emitted when the generation pipeline reaches a terminal state (success or failure).
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    event_type: Literal['generation.completed']
    event_version: Literal[1]
    payload: Payload

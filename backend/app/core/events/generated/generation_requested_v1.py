# generated by datamodel-codegen:
#   filename:  generation.requested.v1.schema.json

from __future__ import annotations

from typing import Literal
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, RootModel, constr


class RequestedModule(RootModel[constr(min_length=1)]):
    root: constr(min_length=1)


class Payload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    job_id: UUID
    application_id: UUID
    user_id: UUID
    requested_modules: list[RequestedModule] = Field(..., min_length=1)


class GenerationRequestedV1(BaseModel):
    """
    Emitted when an application's generation pipeline is enqueued.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    event_type: Literal['generation.requested']
    event_version: Literal[1]
    payload: Payload
